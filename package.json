{
  "name": "codepatch",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "bump": "lerna version --no-private --message 'chore: bump versions'",
    "publish": "lerna publish from-package"
  },
  "private": true,
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.59.5",
    "@typescript-eslint/parser": "^5.59.5",
    "eslint": "^8.40.0",
    "eslint-config-prettier": "^8.8.0",
    "lerna": "^7.2.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
